#include<stdio.h>
#include<conio.h>
void main()
{
int a=1,b=10,n,i,count=0;
printf("\n the first interval is %d",a);
printf("\n the second interval is %d",b);
for(i=2;1<n;i++)
{
if((n%i)==0)
count++;
}
if(count==0)
{
printf("\n the prime number between two intervals are");
}
}
